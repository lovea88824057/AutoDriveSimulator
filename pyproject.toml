[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "worldsimflow"
version = "0.1.0"
description = "A deterministic LogSim and closed-loop RL simulator prototype for autonomous driving simulation."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

